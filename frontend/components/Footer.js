import styles from "../styles/components/Header.module.css"

export default function Footer() {
    return (
        <div className={styles.footerWrapper}>
            <p className={styles.logo}>Sketch Hub</p>
        </div>
    )
}